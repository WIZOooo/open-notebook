# 说明：
# 1) 将本文件复制为同目录下的 docker.env（docker.env 不应提交到仓库，里面会包含密钥）
# 2) 该文件会被 docker compose 通过 env_file 注入到 open_notebook 容器
# 3) 公司服务器部署时，建议配合反向代理（Nginx/Caddy）并启用 OPEN_NOTEBOOK_PASSWORD

# API CONFIGURATION
# 浏览器访问 API 的外部地址（不要以 /api 结尾）
# - 直接在服务器上访问：http://服务器IP:5055
# - 走反代/域名：      https://notebook.example.com
API_URL=http://localhost:5055

# SECURITY（强烈建议生产开启）
# 访问 UI 时需要输入密码（对外网/内网都建议开启）
# OPEN_NOTEBOOK_PASSWORD=please_set_a_strong_password

# AI PROVIDER（至少配置一个，否则无法调用模型）
# 方案 1：OpenAI
OPENAI_API_KEY=

# 方案 2：OpenAI-Compatible（公司内部网关/代理/自建模型）
# OPENAI_COMPATIBLE_BASE_URL=http://your-gateway/v1
# OPENAI_COMPATIBLE_API_KEY=
#
# 可选：为 embedding 单独指定提供商
# OPENAI_COMPATIBLE_BASE_URL_EMBEDDING=http://your-embedding-gateway/v1
# OPENAI_COMPATIBLE_API_KEY_EMBEDDING=

# DATABASE（多容器：用 surrealdb 服务名；不要写 localhost）
SURREAL_URL=ws://surrealdb:8000/rpc

# 重要：
# - 下面账号密码需要与 docker-compose.prod.yml 里 surrealdb 的启动参数保持一致
# - 生产环境务必修改默认 root/root，并同步修改 docker-compose.prod.yml 与本文件
SURREAL_USER=root
SURREAL_PASSWORD=root
SURREAL_NAMESPACE=open_notebook
SURREAL_DATABASE=production
